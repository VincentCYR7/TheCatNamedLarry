#include "King.h"

King::King(const s3d::Vec2& pos, const s3d::Texture& tex)
    : Entity(pos, tex) {}

void King::update(bool scroll) {
    // Custom King update logic here
    // Example: move slowly to the right
    
}
